---
import Icon from "./Icon.astro";

const links = [
  { href: "/", label: "전체 호" },
  { href: "/tags/", label: "태그" },
  { href: "/search/", label: "검색" },
  { href: "/rss.xml", label: "RSS", icon: "rss" },
  { href: "/about/", label: "소개" },
];
---
<footer class="foot-index" data-pagefind-ignore>
  <span><strong>pre-work</strong> · 일 하기 전 10분. 읽고, 하나를 남깁니다.</span>
  <nav class="foot-index__links" aria-label="빠른 이동">
    {links.map((l) => (
      <a href={l.href}>
        {l.icon && <Icon name={l.icon} size={16} />}{l.label}
      </a>
    ))}
  </nav>
</footer>

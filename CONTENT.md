# 저작 규칙

화면을 만들지 않는다. 데이터를 추가하면 화면이 따라온다.
이 문서는 칼럼의 문법과 발행 파이프라인의 규칙을 담는다.

## 절대 넣지 않는 것

이 저장소는 공개 페이지로 배포된다.

- 회사 저장소의 실제 소스, 테이블명, 컬럼명
- 서버 주소, 계정, 비밀번호, 접속 정보, 내부 문서 원문
- 발주처·담당자 개인정보
- 특정 회사·프로젝트를 식별하게 하는 서술 ("우리 팀", 프로젝트명 등)

코드 예시는 **구조만 남기고 이름을 바꿔 각색한다.**
공개된 표준프레임워크 공통컴포넌트 코드(`COMTNBBS` 계열 등)는 공식 배포물이므로 인용해도 된다.

## 칼럼 요청 파이프라인

1. 누구나 `.github/ISSUE_TEMPLATE/publish.yml` 양식으로 칼럼을 요청한다.
2. 운영자가 요청을 검토해 `publish` 라벨을 붙인다 — 라벨이 곧 발행 승인이다.
   템플릿은 라벨을 자동으로 붙이지 않으므로, 승인되지 않은 요청은 발행되지 않는다.
3. 매일 자정 파이프라인이 승인된 이슈를 저작 규칙에 맞는 칼럼으로 발행하고,
   본문 끝에 요청 이슈 링크를 크레딧으로 남긴 뒤 이슈를 닫는다.

## 칼럼 한 편 (`src/data/issues.ts`)

```bash
npm run new:issue
```

한 편의 기준:

- **10분 안쪽.** 지하철에서 다 읽히지 않으면 두 편으로 나눈다.
- **`takeaway` 한 문장.** 오늘 하루 머리에 남길 것 하나. 이것부터 쓰고 본문을 채우면 글이 흩어지지 않는다.
- **`callout` 로 끝낸다.** title은 "오늘 해 볼 것". 독자가 자기 실무에 바로 적용할 확인 가능한 행동 하나.
- **`next` 를 남긴다.** 이어서 읽을 주제를 한 줄로.
- 훈련이 있으면 `link` 블록으로 넘긴다.

## 블록 문법 (`src/data/blocks.ts`)

| 타입 | 쓰는 법 |
| :-- | :-- |
| `p` | 평범한 문단. 사전의 용어가 자동으로 연결된다 |
| `list` | 불릿 목록. `ordered: true` 면 번호 |
| `callout` | 강조 상자. `tone`: `note`/`warn`/`good`. 칼럼의 마무리 블록 |
| `code` | 해설 없는 코드 블록 |
| `codeRead` | 줄을 눌러 해설이 열리는 코드 읽기 블록. `notes[].lines` 는 `"8"` 또는 `"10-15"` |
| `flow` | 화살표로 이어지는 흐름 |
| `tree` | 주석이 달린 파일 트리 |
| `table` | 비교표 |
| `terms` | 사전 항목을 카드로 펼친다. `ids` 는 `glossary.ts` 의 기존 id만 |
| `quiz` | 객관식 자가 점검. 보기는 실제로 헷갈릴 만한 것으로 |
| `link` | 다른 페이지(훈련·출처·요청 이슈)로 넘기는 카드 |

## 코드 읽기 훈련 (`src/data/drills.ts`)

- `level` — 1 읽기(문법과 표시를 알아본다) / 2 추적(화면과 코드를 오간다) / 3 판단(함정을 가려낸다)
- `codeRead` 의 `question` 은 **해설을 열기 전에** 답해 볼 질문으로 쓴다.
- `notes` 의 `lines` 는 `"8"` 또는 `"10-15"`. 1부터 세며, 붙은 줄이 눌러지는 버튼이 된다.
- 한 훈련에 `quiz` 는 한두 개. 보기는 실제로 헷갈릴 만한 것으로 만든다 — 정답이 뻔하면 훈련이 되지 않는다.

## 용어 (`src/data/glossary.ts`)

추가하는 순간 사이트 전체 본문에서 자동으로 연결된다.

| 필드 | 쓰는 법 |
| :-- | :-- |
| `id` | URL 앵커. 케밥케이스. 바꾸면 기존 링크가 깨진다 |
| `term` | 표제어 |
| `aliases` | 본문에서 다르게 쓰이는 표기. 자동 연결과 검색에 함께 쓰인다 |
| `short` | **한 줄.** 호버 툴팁에 그대로 나오므로 두 줄이 되면 잘린 것처럼 보인다 |
| `long` | 사전 페이지에서만 보이는 자세한 설명 |
| `example` | 코드나 설정에서 실제로 보이는 모습 |
| `related` | 함께 볼 용어 id |
| `see` | 더 깊게 다루는 내부 페이지 |
| `noauto` | `true` 면 본문 자동 연결에서 제외 (너무 흔한 단어에 쓴다) |

주의:

- `short` 는 정의만. "왜 중요한가"는 `long` 으로 민다.
- 두 글자 미만은 자동 연결 대상에서 빠진다.
- 흔한 단어를 등록하면 본문이 밑줄투성이가 된다. 애매하면 `noauto: true` 를 준다.

## 이 사이트는 뉴스레터가 기본형이다

읽을거리는 전부 `src/data/issues.ts` 의 칼럼 한 편으로 낸다. 기술 스택·망분리처럼
"찾아볼 때만 펼치는" 사실 정보는 `/stack/`·`/infra/`·`/framework/` 같은 레퍼런스
페이지에 남기고, 칼럼은 그 페이지를 소개·인용하는 얇은 안내로 쓴다 — 스펙 표를
매일 읽는 칼럼 형태로 욱여넣지 않는다. 레퍼런스 페이지를 새로 늘리는 것보다
칼럼을 쓰는 것이 기본 동작이다.

## 문체

- 평서형 종결(`~한다`)로 통일한다.
- 과장하지 않는다. "혁신적", "강력한", "완벽한" 같은 단어는 쓰지 않는다.
- 확실하지 않은 것은 확실하지 않다고 쓴다. 버전·수치는 공식 문서를 확인하고, `link` 블록으로 출처를 남긴다.
- 제목은 짧고 기능적으로. 이탤릭 제목과 장식용 문구는 쓰지 않는다 (`design.md` 참고).
